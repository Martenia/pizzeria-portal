import React from 'react';
// import PropTypes from 'prop-types';
import styles from './New.scss';

const New = () => (
  <div className={styles.component}>
      <h2>New</h2>
      <p>widok składania zamówienia</p>
  </div>
);

export default New;