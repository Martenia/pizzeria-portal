import React from 'react';
// import PropTypes from 'prop-types';
import styles from './New.scss';

const New = () => (
  <div className={styles.component}>
      <h2>New - widok składania zamówienia</h2>    
  </div>
);

export default New;